# =============================================================================
# Resume Builder – environment variables (copy to .env.local or set in Vercel)
# =============================================================================

# -----------------------------------------------------------------------------
# Required for deployment
# -----------------------------------------------------------------------------

# Postgres connection string. Use Neon, Supabase, or any Postgres host.
# Neon: https://neon.tech | Supabase: https://supabase.com
DATABASE_URL="postgresql://user:password@host:5432/dbname?schema=public"

# Clerk (auth). Create an app at https://dashboard.clerk.com
# Enable Google (or other providers) under User & Authentication → Social.
NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...
CLERK_SECRET_KEY=sk_test_...
# Optional: custom sign-in URL (default is /sign-in)
# NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in

# -----------------------------------------------------------------------------
# Optional
# -----------------------------------------------------------------------------

# Daily rate limits per user (defaults: 20 AI improve, 30 PDF export)
# RATE_LIMIT_AI_IMPROVE_PER_DAY=20
# RATE_LIMIT_PDF_EXPORT_PER_DAY=30

# OpenAI: for AI bullet improvement. Server-side only (never exposed to client).
# When set, the Improve button is enabled for all signed-in users.
# OPENAI_API_KEY=sk-your-key-here
